# Minishell
The Mini Shell project simulates a basic Unix shell, handling both built-in and external commands. It supports customizable prompts, job control (bg, fg, jobs), signal handling (SIGINT, SIGTSTP), and process communication using pipes. The shell executes commands, manages background processes, and tracks job statuses.
